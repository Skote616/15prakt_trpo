<Page x:Class="DBFirst.Pages.TagsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DBFirst.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TagsPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListView x:Name="list" Grid.Column="0" HorizontalContentAlignment="Stretch" MouseDoubleClick="EditCard">
            <ListView.ItemTemplate>
                <DataTemplate>

                    <StackPanel Grid.Column="0">
                        <TextBlock FontSize="14" FontWeight="Bold" Text="{Binding Name}"/>
                    </StackPanel>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Button Margin="0,10,0,10" Click="add">Создать</Button>
            <Button Margin="0,10,0,10" Click="remove">Удалить</Button>
            <Button Margin="0,10,0,10" Click="back">Назад</Button>
        </StackPanel>
    </Grid>
</Page>